<%title "Ingredients | #{current_restaurant.name} | Triggy"%>
<%page_name "Ingredients"%>
<div class="row">
  <div class="col-12">
    <%= link_to new_restaurant_ingredient_path, class: "btn btn-info btn-sm mb-3 float-right" do%>
    <em class="fas fa-plus"></em> New ingredient
    <% end -%>
    <div class="table-responsive">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th width="5%">S.No</th>
            <th>Name</th>
            <th>Unit</th>
            <th width="10%"></th>
          </tr>
        </thead>
        <tbody>
          <%if @ingredients.present?%>
          <%i = (page - 1) * per_page%>
          <% @ingredients.each do |ingredient| %>
          <tr>
            <td><%= i+= 1%></td>
            <td>
              <%= ingredient.name %>
            </td>
            <td>
              <%= ingredient.unit %>
            </td>
            <td>
              <%= link_to edit_restaurant_ingredient_path(ingredient), class: "btn btn-sm btn-warning" do %>
                <em class="fas fa-edit"></em>
              <% end -%>
              <%= link_to [:restaurant, ingredient], class: "btn btn-sm btn-danger", method: :delete, data: {confirm: "Are you sure you want to delete this ingredient?"} do %>
                <em class="fas fa-trash"></em>
              <% end -%>
            </td>
          </tr>
          <% end %>
          <%else%>
          <tr>
            <td colspan="7">
              <h4 class="text-danger text-center">No Records Found!</h4>
            </td>
          </tr>
          <% end -%>
        </tbody>
      </table>
      <%= paginate @ingredients, theme: 'twitter-bootstrap-4', :pagination_class => "pagination justify-content-center" %>
    </div>
  </div>
</div>